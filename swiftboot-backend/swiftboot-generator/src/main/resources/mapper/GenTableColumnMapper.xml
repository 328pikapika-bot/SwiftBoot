<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swiftboot.generator.mapper.GenTableColumnMapper">

    <select id="selectDbTableColumnsByName" resultType="com.swiftboot.generator.domain.GenTableColumn">
        SELECT column_name AS columnName,
               (CASE WHEN (is_nullable = 'no' AND column_key != 'PRI') THEN '1' ELSE NULL END) AS isRequired,
               (CASE WHEN column_key = 'PRI' THEN '1' ELSE '0' END) AS isPk,
               ordinal_position AS sort,
               column_comment AS columnComment,
               (CASE WHEN extra = 'auto_increment' THEN '1' ELSE '0' END) AS isIncrement,
               column_type AS columnType
        FROM information_schema.columns
        WHERE table_schema = (SELECT DATABASE())
          AND table_name = #{tableName}
        ORDER BY ordinal_position
    </select>

    <select id="selectByTableId" resultType="com.swiftboot.generator.domain.GenTableColumn">
        SELECT * FROM gen_table_column WHERE table_id = #{tableId} ORDER BY sort
    </select>

    <delete id="deleteByTableId">
        DELETE FROM gen_table_column WHERE table_id = #{tableId}
    </delete>

</mapper>
