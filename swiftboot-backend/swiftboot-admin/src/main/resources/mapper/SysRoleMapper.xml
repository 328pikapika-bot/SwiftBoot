<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swiftboot.admin.mapper.SysRoleMapper">

    <select id="selectRolesByUserId" resultType="com.swiftboot.admin.domain.entity.SysRole">
        SELECT r.*
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.deleted = 0 AND r.status = 0
    </select>

    <select id="selectByRoleKey" resultType="com.swiftboot.admin.domain.entity.SysRole">
        SELECT * FROM sys_role WHERE role_key = #{roleKey} AND deleted = 0
    </select>

</mapper>
